// Strings em C#

StartsWith = Comeca com ...

EndsWith = Termina com ...

Guids = indentificador unico (numero gigante que nao se repete)

Equals = Comparar 

Contains = variavel contem ...

CompareTo = comparar variaveis e retornar um inteiro


Indices = usados em litas, string e uma lista de caracteres q formam uma string

IndexOf = conta a posicao do caracter que vc indica

LastIndexOf = ultimo indice que ele encontrar na string 

ToLower = converter o texto pra Minusculo

ToUpper = converter o texto pra Maiusculo 

Insert = inserir um caracter no texto (indice, "O texto que vc quer inserir")

Remove = remover um caracter do texto (indice pra comecar, quantidade de indices que vc quer remover)

Length = quantidade de caracter q uma string tem

Replace = Trocar caracteres na strings

Split = dividir caracteres da strings, vai retornar uma lista da string 

Substring = pega um caracter especifico, determinando a posicao e a qnt de caracter depois dessa posicao

Trim = limpar os espacos do comeco e final 

StringBuilder = dentro do (System.text), var.Append => quebrar linha e entender como uma linha so
